bits 16
mov sp, di
mov bp, ax
mov bp, [bx+si+16]
mov [bx+16], bp
mov bp, [32]
mov [32], bp
mov cx, bx
mov ch, ah
mov dx, bx
mov si, bx
mov bx, di
mov al, cl
mov ch, ch
mov bx, ax
mov bx, si
mov cx, 5
mov cx, -5
