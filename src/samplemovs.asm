bits 16
mov sp, di
mov bp, ax
mov bp, [bx+si+16]
mov [bx+16], bp
mov bp, [32]
mov [32], bp
mov cx, bx
mov ch, ah
mov dx, bx
mov si, bx
mov bx, di
mov al, cl
mov ch, ch
mov bx, ax
mov bx, si
mov cx, 5
mov cx, -5
mov dx, 3948
mov dx, -3948
mov dx, [bp]
mov al, [bx+si]
mov bx, [bp+di]
mov ah, [bx+si+4]
mov al, [bx+si+4999]
mov [bx+di], cx
mov [bp+si], cl
mov [bp], ch
mov bp, [5]
mov bx, [3458]
mov ax, [2555]
mov [2555], ax
mov ax, [16]
mov [15], ax
mov [2554], ax
mov ax, [bx+di-37]
mov dx, [bx-32]
mov [si-300], cx
mov [bp+di], byte 7
mov [di+901], word 347

